package com.example.training2.collections
/*
class Book(
    val title: String,
    val author: String
)

class Library {
    private val books = mutableListOf<Book>()

    fun addBook(book: Book) {
        books.add(book)
        println("Book ${book.title} add to library")
    }
}

fun listBooks() {
    if (books.isEmpty()) {
        println("Library is empty")
    } else {
    println("List of books in library:")
        for ((index, book) in books.withIndex()) {
            println("${index + 1}. \"${book.title}\" автор: ${book.author}")
         }
     }
  }


}

fun main() {
    val library = Library()
    val book1 = Book("War and Peace", "Lev Tolstoy")
    val book2 = Book("Master and Margarita", "Mikhail Bulgakov")

    library.addBook(book1)
    library.addBook(book2)

    library.listBooks()
}
*/